# Example environment variables for EcoLog
# Copy this file to `.env` and fill in real values. Do NOT commit secrets to version control.

# Database
# A Postgres connection string (Postgres / Neon / Heroku style)
# Example: postgresql://db_user:db_pass@db_host:5432/db_name
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Replit OIDC client id used for authentication and some dev plugins
# If you're running locally and not using Replit OIDC, you can leave this blank.
REPL_ID=your-repl-id

# Session secret for express-session (use a secure random string)
SESSION_SECRET=replace_with_a_secure_random_string

# OpenID Connect issuer discovery URL. Defaults to Replit in code.
# Example: https://replit.com/oidc
ISSUER_URL=https://replit.com/oidc

# Server
# Port the server should listen on. Defaults to 5000 in code if unspecified.
PORT=5000

# Node environment
# Use 'development' locally and 'production' in deployed environments
NODE_ENV=development

# Notes:
# - After copying to .env, make sure .env is listed in your .gitignore.
# - Keep secrets (DATABASE_URL, SESSION_SECRET, REPL_ID) private.
